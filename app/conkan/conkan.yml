# rename this file to conkan.yml and put a ':' after 'name' if
# you want to use YAML like in old versions of Catalyst
name: conkan

Model::ConkanDB:
  schema_class: conkan::Schema
  connect_info:
    dsn: dbi:mysql:
    user: 
    password: 
    mysql_enable_utf8: 1
    on_connect_do: SET NAMES utf8
